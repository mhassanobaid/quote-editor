<% if policy(quote).destroy? %>
  <%= button_to "Delete",
                quote_path(quote),
                method: :delete,
                class: "btn btn--light" %>
<% end %>

<% if policy(quote).edit? %>
  <%= link_to "Edit",
              edit_quote_path(quote),
              class: "btn btn--light" %>
<% end %>
